---
track-id: {track_id}
owner-container: {owner_container}
phase: {phase}
status: active
updated-at: {updated_at_iso}
---

# {project} — Session Handoff ({date}) — {cycle_name}

<!--
TEMPLATE INSTRUCTIONS (delete after fill):

Generated by phase-d-closer D.3 step (aria-plugin v1.21.0+ Rule #9 core;
§2.3 frontmatter schema added in aria-plugin v1.22.x+ per OpenSpec
multi-terminal-coordination, DEC-20260519-001).

═══════════════════════════════════════════════════════════════════════════
FRONTMATTER fields (lines 1-7 above, machine-readable, MUST fill all 5)
Schema canonical source: `standards/conventions/session-handoff.md` §2.3.1
═══════════════════════════════════════════════════════════════════════════

  {track_id}        — 确定性派生的工作 ID,与该 handoff 所属 OpenSpec change /
                      carry-forward 条目 1:1 绑定。
                      规范化函数 (跨容器必须共用):
                        1. 小写化
                        2. `/` `.` `_` 替换为 `-`
                        3. 最大长度 64 字符
                        4. 超长或含非 ASCII 时 fallback `sha256(原 id)[:16]`
                      示例: "multi-terminal-coordination"
                            "aria-2-0-m5-carryover-layer2-redo-mode-aux"

  {owner_container} — `<owner>/<container-id>` 复合标识
                      ⚠️ **机械填, 勿手动组装** (DEC-20260704-002 §4, 病根 #3):
                      手填曾漂移出 6 种不一致值 (同一容器 3 种主串), 破坏 collision 分类。
                      逐字粘贴以下命令输出 (get_identity, 确定性):
                        python3 aria/skills/session-closer/scripts/handoff_autofill.py --owner-container
                      <owner>        = git user.email 的 local-part (`@` 之前)
                      <container-id> = `~/.aria/container-id` 内容
                                       (持久 short-UUID + 可选人类标签;
                                        文件缺失时回退 hostname)
                      示例: "creationhikari/devbox-A"
                            "simonfish/bfe8285d"  (label 空 → uuid; 设 label 使更可读)

  {phase}           — 写出时该 track 所处的十步循环阶段。
                      enum: A / A.1 / A.2 / A.3
                          / B / B.1 / B.2 / B.3
                          / C / C.1 / C.2
                          / D / D.1 / D.2 / D.3
                      允许子阶段 progress 后缀: "B 7/9", "A.2 R1+R2"

  status            — 通常写出时填 "active" (在飞,有 carry-forward)
                      其它枚举值(由 phase-d-closer 根据 cycle 完整度判定):
                        "done"      — 全 cycle 完成,session 收尾归档
                        "abandoned" — 放弃,carry-forward 转他人或废弃

  {updated_at_iso}  — UTC ISO 8601 秒精度。生成命令:
                        date -u +'%Y-%m-%dT%H:%M:%SZ'
                      示例: "2026-05-19T22:31:13Z"

═══════════════════════════════════════════════════════════════════════════
PROSE variables to substitute (§0-§8 narrative below):
═══════════════════════════════════════════════════════════════════════════

  {project}              — e.g. "Aria" or "SilkNode"
  {date}                 — YYYY-MM-DD (UTC) when session closed
  {cycle_name}           — short slug identifying primary cycle
                           (e.g. "H0 phase-a-done", "M5 phase-b-1")
  {session_duration}     — actual elapsed hours (e.g. "~12h")
  {shipped_cycles}       — count or list of cycles fully completed this session
  {memory_entries_count} — number of new auto-memory entries this session (§8)
  {next_session_entry}   — primary command/skill to start next session
                           (usually /aria:state-scanner)

Canonical output path (hardcoded per H0 spec, do not change):
  docs/handoff/{YYYY-MM-DD}-{slug}.md
  (if same-day duplicate: docs/handoff/{YYYY-MM-DD}-{HHMM}-{slug}.md)

After writing this file:
- **Single-track case** (1 active track): update `docs/handoff/latest.md`
  pointer to this file (向后兼容老 session 读)
- **Multi-track case** (≥2 active tracks per `state-scanner` 看板):
  `latest.md` 不写真实指针,仅含 deprecation banner 指引读多 track 看板
  (per `multi-terminal-coordination` tasks.md 1.6)
- state-scanner Phase 1 will auto-parse 上面的 frontmatter 重建多 track 看板
  on next fetch (per `multi-terminal-coordination` tasks.md 1.3-1.5)

注意: prose §0-§8 与 frontmatter 共存,frontmatter 是机读 SOT,prose 是人读
SOT,两者不冲突 (per standards/conventions/session-handoff.md §2.3.3)。
-->

> **Status**: Active — Ready for next session
> **Cycle period**: {start_date} → {date}
> **Next session 入口**: 优先读本 doc → `{next_session_entry}` 自动恢复 → §6 选择下一步

---

## §0 入口 (新 session 优先读)

新 session 应:

1. 运行 `{next_session_entry}` — 项目状态扫描入口
2. **state-scanner v3.0+ Phase 1.15 `handoff` 字段会自动 surface 本 doc 路径** — AI 在阶段 2 推荐前必读 (per H0 spec, aria-plugin v1.21.0+)
3. 按 §6 "Next session 入口" 优先级建议执行

如果本 doc surfaced **失败** (e.g. snapshot.handoff 字段缺失):
- 检查 `aria-plugin` 是否升级到 v1.21.0+
- 手动 `ls docs/handoff/ | tail -1` fallback

---

## §1 已完成 (按时间顺序)

<!-- 列出本 session 完成的全部里程碑，附 commit SHA + relevant PR/issue 链接 -->

| 时间 | 事件 | Commit / PR | 备注 |
|------|------|-------------|------|
| {YYYY-MM-DD HH:MM} | {event description} | {sha} / {pr_url} | {notes} |
| ... | ... | ... | ... |

**Cycles shipped this session**: {shipped_cycles}

---

## §2 未完成 / Carry-forward 清单

### 高优先级 (建议下次 session 优先评估)

| # | 项目 | scope | 估时 | 来源 |
|---|------|-------|------|------|
| H1 | {item} | {description} | {hours} | {source: spec / issue / decision} |
| H2 | ... | ... | ... | ... |

### 中优先级

| # | 项目 | 状态 | 备注 |
|---|------|------|------|
| M1 | ... | {pending / in_progress} | ... |

### 低优先级 / cleanup

- {item 1}
- {item 2}

---

## §3 关键风险 / 已知陷阱

<!-- 本 session 发现但未完全解决的风险点；新 session 应避免重蹈覆辙 -->

| 风险 | 触发条件 | 缓解 / workaround |
|------|----------|-------------------|
| {risk description} | {when it bites} | {how to avoid or recover} |

---

## §4 实战教训 (memory 沉淀来源)

<!-- 本 session 学到的"非显然"的方法论或技术要点；如果值得跨 session 保留，应转化为 auto-memory entry (§8) -->

- **{lesson title}**: {lesson body — what was tried, what worked, why it's non-obvious}
- ...

---

## §5 多维度同步状态 (Aria 4 维度 或对应外部系统)

| 维度 | 本 session 涉及? | 状态 | 备注 |
|------|------------------|------|------|
| UPM (进度) | {yes/no} | {state} | {notes} |
| User Stories | {yes/no} | {state} | {notes} |
| OpenSpec | {yes/no} | {state, with archive paths if applicable} | {notes} |
| PRD | {yes/no} | {state} | {notes} |
| Standards / conventions | {yes/no} | {state} | {notes} |
| Skill docs | {yes/no} | {state} | {notes} |
| Auto-memory | {yes/no} | {count} new entries | 见 §8 |
| Decision memos | {yes/no} | {paths} | ... |
| Audit reports | {yes/no} | {count + paths} | ... |
| CHANGELOG | {yes/no} | {version entry} | ... |

<!-- 删除项目不适用的行 (e.g. 项目无 UPM 时删除该行) -->

---

## §6 Next session 入口 + 优先级建议

```bash
{next_session_entry}
```

**优先级建议** (按本 session 判断, 新 session 可调整):

每条 carry-forward = `{id, desc}` (prose markdown, **非 frontmatter**;schema 详见
`standards/conventions/session-handoff.md` §2.3):

- `id` — kebab 格式 `carry-<slug>` (**禁用 `:`** — `derive_track_id` 归一时不译冒号)。
  若该条与本 handoff frontmatter `track-id` 指向同一工作,**直接复用该原始串**
  (防同一工作被算作两条 track;复用时**不强制** `carry-` 前缀,前缀仅新起 carry-id
  时的可读性约定)
- `desc` — 优先级理由 + 估时 + 类型 (即下方 {rationale, est hours, type} 内容)
- 本 §6 carry-id 是 **human-curated prose 层**,与 §2 `carry_forward_inventory`
  (机读, tasks.md inline 注解自动汇入,当前占位 — id 化留 #95 follow-up) **分工不同,
  互不替代**

1. ⭐ **`{id: carry-<slug>}`** {top priority item} — {rationale, est hours, type}
2. **`{id: carry-<slug>}`** {second priority} — ...
3. **`{id: carry-<slug>}`** {third priority} — ...

**不应该做的**:
- 不要重复 {recently completed work}
- 不要再 triage 已 closed issue {N}
- 其他 anti-patterns

---

## §7 提交清单 (commit hash + multi-remote parity)

```
[{repo_1}]   master = {sha} | origin = github ✅
[{repo_2}]   master = {sha} | origin = github ✅
[{repo_3}]   master = {sha} | origin = github ✅
```

**Tags published**: {v1.X.Y if any}
**PRs merged**: {pr_urls}

---

## §8 Memory entries this session ({memory_entries_count} new)

<!-- 列出本 session 新建的 auto-memory entries (project/feedback/reference/user 各类) -->

| File | Type | Theme |
|------|------|-------|
| [{slug}.md]({path}) | {project/feedback/reference/user} | {one-line theme} |
| ... | ... | ... |

加上前期 session N 个 — MEMORY.md 全部 indexed。

---

## Cross-references

- [Brainstorm decision](../../.aria/decisions/{date}-{slug}.md) — {brief}
- [Approval signoff](../../.aria/decisions/{date}-{spec}-approved.md) — {brief}
- [R1+R2 audit reports](../../.aria/audit-reports/post_spec-{R1,R2}-{date}-{spec}-*.md)
- [proposal.md](../../openspec/changes/{spec}/proposal.md) | [tasks.md](../../openspec/changes/{spec}/tasks.md)
- [Related external issue](https://forgejo.10cg.pub/{org}/{repo}/issues/{N})
- [Predecessor handoff]({prev_handoff_path})

---

**Created**: {date} EOD
**Session duration**: {session_duration}
**Status**: Active — {what next session should focus on}
