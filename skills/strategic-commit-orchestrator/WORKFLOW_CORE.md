# 工作流程 - 通用部分

> **版本**: 2.2.0
> **职责**: Phase 0-7 通用工作流程

本文档包含所有变更类型共用的工作流程步骤。

**📚 文档导航**:
- **入口文档** → [SKILL.md](./SKILL.md)
- **变更类型识别** → [CHANGE_TYPES.md](./CHANGE_TYPES.md)
- **类型特定流程** → WORKFLOW_TYPE_*.md
- **子模块指南** → [SUBMODULE_GUIDE.md](./SUBMODULE_GUIDE.md) (v2.2.0新增)
- **通用流程（本文档）** → 您正在阅读
- **故障处理** → [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)

---

## 流程概览

```
┌─────────────────────────────────────────────────────────┐
│  Phase 0: 全项目状态扫描  ← 仅类型E，见 TYPE_E.md       │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Phase 1: 项目状态感知  ← 见 WORKFLOW_TYPE_*.md         │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Phase 2: 变更智能分析  ← 本文档                        │
│  Phase 3: 分组提交策略  ← 本文档                        │
│  Phase 4: Subagent分配  ← 本文档                        │
│  Phase 5: 并行执行编排  ← 本文档                        │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Phase 6: 提交消息增强  ← 6.1-6.2本文档, 6.3见TYPE_*   │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Phase 7: 执行验证监控  ← 本文档                        │
└─────────────────────────────────────────────────────────┘
```

---

## Phase 0: 全项目状态扫描 (仅类型E)

> **适用**: 仅当识别为类型E（全项目变更）时执行此阶段

类型E是指主项目和至少一个子模块同时有变更的情况。

**详细流程**: 见 [WORKFLOW_TYPE_E.md](./WORKFLOW_TYPE_E.md)

**快速参考**:
```bash
# 扫描主项目变更
git status --short --ignore-submodules=dirty

# 扫描子模块变更
git submodule foreach --quiet '
  changes=$(git status --short 2>/dev/null | wc -l)
  if [ "$changes" -gt 0 ]; then
    echo "$name:$changes"
  fi
'
```

---

## Phase 2: 变更智能分析

### 步骤2.1: 获取所有未提交变更

```bash
# 检查git状态
git status

# 获取变更文件列表
git diff --name-status

# 获取暂存区变更
git diff --staged --name-status

# 查看详细变更统计
git diff --stat
git diff --staged --stat
```

**输出示例**:
```
M  .claude/skills/architecture-doc-updater/SKILL.md
A  backend/BACKEND_ARCHITECTURE.md
A  backend/llm_provider/LLM_PROVIDER_ARCHITECTURE.md
A  backend/app/APP_ARCHITECTURE.md
A  backend/docs/architecture/backend-overview.md
M  backend (submodule)
```

### 步骤2.2: 变更文件分类分析

```yaml
分类规则:

  1. 架构文档类:
     匹配: *_ARCHITECTURE.md (大写，代码目录中)
     特征: 位于代码目录，遵循L0/L1/L2层次
     分组: 按端级(L0)、模块级(L1)分组

  2. 设计文档类:
     匹配: docs/architecture/*.md (小写，docs目录中)
     特征: 详细设计文档、技术方案
     分组: 可与架构文档合并或独立

  3. Skill文档类:
     匹配: .claude/skills/*/SKILL.md
     特征: AI Skill 定义和指导
     分组: 按Skill独立分组

  4. 功能代码类:
     匹配: lib/**/*.dart, app/**/*.py
     特征: 业务逻辑实现
     分组: 按模块(backend/mobile/shared)分组

  5. 测试代码类:
     匹配: test/**/*_test.dart, tests/**/*.py
     特征: 单元测试、集成测试
     分组: 与对应功能代码关联或独立

  6. 配置文件类:
     匹配: *.yaml, *.json, .gitignore等
     特征: 项目配置和依赖
     分组: 独立分组或与相关功能合并

  7. Submodule变更:
     匹配: backend/, mobile/, shared/
     特征: Git submodule 指针变更
     分组: 通常独立提交说明submodule更新
```

### 步骤2.3: 依赖关系分析

```yaml
依赖分析原则:

  独立变更 (可并行):
    - 不同模块的文档变更
    - 不同Skill的独立更新
    - 不同端的代码变更 (backend vs mobile)

  依赖变更 (必须顺序):
    - 代码 → 测试 (先提交代码，再提交测试)
    - API定义 → 实现 (先API契约，再实现)
    - 架构文档 → 实现代码 (先文档，再代码)
    - Submodule更新 → 主项目引用 (先子模块，再主项目)

  关联变更 (建议合并):
    - 功能实现 + 对应测试
    - 架构文档 + 设计文档 (同一主题)
    - 代码重构 + 相关文档更新
```

---

## Phase 3: 分组提交策略设计

### 策略3.1: 架构文档分组策略

```yaml
Backend架构文档示例:

Group 1: Backend端级架构 (L0)
  文件:
    - backend/BACKEND_ARCHITECTURE.md
  类型: docs
  scope: backend
  优先级: P0 (基础，其他依赖)

Group 2: Backend模块架构 (L1)
  文件:
    - backend/llm_provider/LLM_PROVIDER_ARCHITECTURE.md
    - backend/app/APP_ARCHITECTURE.md
  类型: docs
  scope: backend
  优先级: P1 (依赖Group 1)

Group 3: Backend设计文档
  文件:
    - backend/docs/architecture/backend-overview.md
    - backend/docs/architecture/llm-provider-architecture.md
  类型: docs
  scope: backend
  优先级: P2 (补充说明)

并行可行性:
  Group 1 独立提交: ✅ 可以
  Group 2+3 并行: ✅ 可以 (不依赖对方)

建议策略:
  - 串行: Group 1 → Group 2 → Group 3
  - 或并行: Group 1 → [Group 2 || Group 3]
```

### 策略3.2: 功能开发分组策略

```yaml
多端功能开发示例:

Group A: Backend API实现
  文件:
    - backend/app/api/chat.py
    - backend/app/services/chat_service.py
    - backend/app/models/chat.py
  类型: feat
  scope: backend/api
  agent: backend-architect

Group B: Mobile UI实现
  文件:
    - mobile/lib/pages/chat_page.dart
    - mobile/lib/widgets/message_bubble.dart
  类型: feat
  scope: mobile/ui
  agent: mobile-developer

Group C: 共享数据模型
  文件:
    - shared/models/message.dart
    - shared/contracts/chat_api.yaml
  类型: feat
  scope: shared/api
  agent: api-documenter

Group D: 测试套件
  文件:
    - backend/tests/test_chat_api.py
    - mobile/test/pages/chat_page_test.dart
  类型: test
  scope: integration
  agent: qa-engineer

并行可行性:
  C → [A || B] → D
  (共享模型先行，Backend和Mobile并行，最后集成测试)
```

### 策略3.3: Skill更新分组策略

```yaml
Skill独立性原则:
  - 每个Skill独立提交
  - 不与其他类型变更混合
  - 清晰的变更说明和版本

示例:

Group: architecture-doc-updater Skill更新
  文件:
    - .claude/skills/architecture-doc-updater/SKILL.md
  类型: docs
  scope: skills/architecture
  描述: 集成v4.5架构文档管理系统
  agent: knowledge-manager
```

### 策略3.4: 分组命名规范

```yaml
分组命名模板:
  "Group {序号}: {模块名}-{变更类型}-{简短描述}"

示例:
  - "Group 1: Backend-L0-Architecture"
  - "Group 2: Backend-L1-Module-Architecture"
  - "Group 3: Backend-Design-Docs"
  - "Group A: Backend-API-Chat-Feature"
  - "Group B: Mobile-UI-Chat-Feature"
```

---

## Phase 4: Subagent 智能分配

### 分配4.1: Subagent 能力映射

```yaml
可用Subagent清单:

1. knowledge-manager:
   专长: 架构文档、知识库管理、文档质量审核
   适用:
     - *_ARCHITECTURE.md 文件
     - docs/architecture/*.md 文件
     - 文档体系建设
   示例: "创建Backend架构文档体系"

2. backend-architect:
   专长: 后端系统设计、API架构、数据库设计
   适用:
     - backend/**/*.py 代码
     - API实现
     - 数据模型定义
   示例: "实现聊天API端点"

3. mobile-developer:
   专长: Flutter/Dart开发、移动UI、状态管理
   适用:
     - mobile/lib/**/*.dart 代码
     - 移动端功能
     - UI组件
   示例: "实现聊天页面UI"

4. api-documenter:
   专长: API文档、OpenAPI规范、SDK生成
   适用:
     - shared/contracts/*.yaml
     - API契约文档
     - 接口定义
   示例: "定义Chat API契约"

5. qa-engineer:
   专长: 测试策略、质量保证、缺陷分析
   适用:
     - test/**/*_test.dart
     - tests/**/*.py
     - 测试套件
   示例: "添加聊天功能集成测试"

6. tech-lead:
   专长: 技术决策、架构评审、重构规划
   适用:
     - 重大重构
     - 架构调整
     - Skill开发
   示例: "重构认证模块架构"

7. general-purpose:
   专长: 通用任务、代码搜索、文件操作
   适用:
     - 配置文件修改
     - 简单文档更新
     - 杂项变更
   示例: "更新.gitignore配置"
```

### 分配4.2: 分配决策树

```yaml
决策流程:

IF 文件匹配 *_ARCHITECTURE.md OR docs/architecture/*.md:
  → ASSIGN: knowledge-manager

ELSE IF 文件匹配 backend/**/*.py:
  IF 变更类型 = feat OR refactor:
    → ASSIGN: backend-architect
  ELSE IF 变更类型 = test:
    → ASSIGN: qa-engineer

ELSE IF 文件匹配 mobile/lib/**/*.dart:
  IF 变更类型 = feat OR refactor:
    → ASSIGN: mobile-developer
  ELSE IF 变更类型 = test:
    → ASSIGN: qa-engineer

ELSE IF 文件匹配 shared/contracts/*.yaml:
  → ASSIGN: api-documenter

ELSE IF 文件匹配 test/**/* OR tests/**/*:
  → ASSIGN: qa-engineer

ELSE IF 文件匹配 .claude/skills/**:
  → ASSIGN: tech-lead

ELSE:
  → ASSIGN: general-purpose
```

---

## Phase 5: 并行执行编排

### 编排5.1: 执行策略设计

```yaml
执行策略类型:

1. 全并行策略 (Fast Track):
   条件: 所有分组完全独立，无依赖关系
   执行: 同时启动所有Subagent Task
   示例: 3个不同Skill的独立更新

2. 阶段并行策略 (Phased):
   条件: 存在阶段性依赖
   执行: 按Phase分批并行
   示例:
     Phase 1: [Group A || Group B]  # 并行
     Phase 2: [Group C]              # 依赖A+B

3. 串行策略 (Sequential):
   条件: 强依赖关系，必须顺序执行
   执行: 逐个完成
   示例: API定义 → 实现 → 测试

4. 混合策略 (Hybrid):
   条件: 部分并行 + 部分串行
   执行: 灵活组合
   最常用: ✅
```

### 编排5.2: Task工具调用规范

**全并行示例**:
```yaml
# 3个独立Skill更新，完全并行

执行命令:
  使用单次Tool调用，包含3个Task:

Task 1:
  subagent_type: knowledge-manager
  description: "Commit architecture-doc-updater Skill"
  prompt: |
    使用 commit-msg-generator 生成提交消息并执行git commit。

    文件: .claude/skills/architecture-doc-updater/SKILL.md
    变更类型: docs(skills/architecture)
    描述: 集成v4.5架构文档管理系统

    生成的消息需包含:
    - 🤖 Executed-By: knowledge-manager subagent
    - 📋 Context: {从Phase 1获取}
    - 遵循git-rule.mdc双语规范
```

**阶段并行示例**:
```yaml
# Phase 1: 并行执行A和B
# Phase 2: 串行执行C (依赖A+B)

Phase 1执行:
  Task A: backend-architect → Backend API实现
  Task B: mobile-developer → Mobile UI实现

  等待: Phase 1所有Task完成

Phase 2执行:
  Task C: qa-engineer → 集成测试

  等待: Task C完成

完成: 所有变更已提交
```

### 编排5.3: 并发控制和错误处理

```yaml
并发控制:
  最大并行数: 建议不超过5个Task
  原因: 避免git冲突和资源竞争

错误处理策略:

  1. 单Task失败:
     策略: 继续其他Task，记录失败任务
     后处理: 手动修复失败任务后重试

  2. 关键Task失败:
     策略: 取消后续依赖Task
     示例: Phase 1失败 → 取消Phase 2

  3. Git冲突:
     策略: 暂停所有Task，解决冲突后重试

  4. 部分成功:
     策略: 已成功的不重复，仅重试失败的
```

---

## Phase 6: 提交消息增强 (通用部分)

### 增强6.1: 基础消息生成

**调用 commit-msg-generator**:
```yaml
步骤:
  1. 分析变更文件和内容
  2. 确定type和scope
  3. 生成符合Conventional Commits的基础消息
  4. 遵循git-rule.mdc双语规范

基础消息示例:
  docs(backend): 创建Backend架构文档体系 / Create Backend architecture documentation system

  完成Backend L0/L1/L2三层架构文档创建。

  - BACKEND_ARCHITECTURE.md (L0端级架构)
  - LLM_PROVIDER_ARCHITECTURE.md (L1模块架构)
  - APP_ARCHITECTURE.md (L1模块架构)
```

### 增强6.2: 添加Agent和Context标记

**增强标记格式**:
```yaml
标记规范:
  🤖 Executed-By: {subagent_type} subagent
  📋 Context: {Phase}-{Cycle} {brief_context}
  🔗 Related: {related_tasks} (可选)

位置: 消息Body之后，Footer之前
```

### 增强6.3: 项目进度关联

⚠️ **此部分按类型不同处理，请参考对应的 WORKFLOW_TYPE_*.md 文档**

---

## Phase 7: 执行验证和监控

### 验证7.1: 提交前检查

```yaml
检查清单:

  ✅ Git状态检查:
    - 暂存区有待提交文件
    - 无合并冲突
    - 分支状态正常

  ✅ 消息格式检查:
    - 符合git-rule.mdc规范
    - 包含双语描述
    - type和scope正确

  ✅ Agent标记检查:
    - 包含 Executed-By 标记
    - Context 信息准确
    - 关联信息完整

  ✅ 分组策略检查:
    - 文件分组合理
    - 依赖关系正确
    - 并行策略可行
```

### 验证7.2: 提交后验证

```bash
# 验证提交成功
git log -1 --oneline

# 检查提交详情
git show HEAD

# 验证分支状态
git status

# 检查提交历史
git log --oneline --graph -10
```

### 验证7.3: 并行执行监控

```yaml
监控指标:
  - Task启动成功率
  - Task执行时间
  - Git提交成功率
  - 错误和冲突次数

监控方式:
  - 使用AgentOutputTool获取Task状态
  - 检查git log验证提交
  - 记录执行日志
```

---

*本文档是 strategic-commit-orchestrator v2.1.0 的通用工作流程指南。*
